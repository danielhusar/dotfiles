npm install -g gulp
npm install -g grunt
npm install -g mocha
npm install -g yo
npm install -g vtop
npm install -g bower
npm install -g generator-simple-node-package
npm install -g generator-gulp-plugin-boilerplate